interface:
  display_name: "Engineering Architecture"
  short_description: "Govern ownership, boundaries, and component cohesion"
  default_prompt: "Use $engineering-architecture to review this non-trivial change for module and component ownership, then implement it without overengineering or creating a mega-component."
policy:
  allow_implicit_invocation: true
